---
title: Pick a backend. Bring your own.
eyebrow: Adapters
description: Per-adapter config, auth, and gotchas for every first-party storagesdk backend.
section: adapters
---

import Note from '../../components/docs/Note.astro';

Per-adapter config, auth, and gotchas. Snapshots and forks are native on Tigris and
emulated as sibling buckets on every other backend.

<Note>
  This page is a placeholder while the docs are being written. The sidebar on the
  left shows the structure that will be filled in.
</Note>
