---
title: API reference.
eyebrow: API
description: Every method, every option, every return type of the storagesdk API.
section: api
---

import Note from '../../components/docs/Note.astro';

Every method, every option, every return type. The same shape on every adapter.

<Note>
  This page is a placeholder while the docs are being written. The sidebar on the
  left shows the structure that will be filled in.
</Note>
