# @storagesdk/core

Core types and runtime for [storagesdk](https://github.com/tigrisdata/storagesdk).

One API across object storage providers, with fork and snapshot built in.

> Status: pre-release. The public API lands in phase 1. See `docs/RFC.md` and `docs/PLAN.md` at the repo root.
