---
title: The same surface, from your shell.
eyebrow: CLI
description: '@storagesdk/cli — the same surface, from your shell.'
section: cli
---

import Note from '../../components/docs/Note.astro';

`@storagesdk/cli` wraps every method as a command. Pipe with stdin/stdout, switch
adapters with `--adapter`, get JSON by default.

<Note>
  This page is a placeholder while the docs are being written. The sidebar on the
  left shows the structure that will be filled in.
</Note>
